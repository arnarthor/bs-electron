{
  "name": "bs-electron",
  "version": "1.0.0",
  "description": "Bucklescript bindings for Electron",
  "main": "index.bs.js",
  "scripts": {
    "build": "bsb -clean-world -make-world",
    "clean": "bsb -clean-world",
    "watch": "bsb -clean-world -make-world -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "arnarthor",
  "license": "MIT",
  "devDependencies": {
    "bs-platform": "^4.0.5"
  },
  "dependencies": {
    "electron": "^2.0.8"
  }
}
